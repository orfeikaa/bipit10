@model Avto
@{
    ViewBag.Title = "EditAvto";
}

@using (Html.BeginForm())
{
    <fieldset>
        @Html.HiddenFor(m => m.Id)
        <p>
            @Html.LabelFor(m => m.Model, "Автомобиль")
            <br />
            @Html.EditorFor(m => m.Model)
        </p>
        <p><input type="submit" value="Редактировать" /></p>
    </fieldset>
}

